<%# Andamio de F3: la pantalla existe y navega, pero la construye un bloque
    posterior. Se pinta explícito en vez de dejar un hueco en blanco, porque un
    hueco parece un error y esto es una fecha. %>
<div class="flex-1 flex items-center justify-center">
  <p class="text-t12 text-terminal-disabled">
    <%= title %> · se construye en el bloque <%= block %>
  </p>
</div>
